# This is a Declarative Automation Bundle definition for Databricks_Asset_Bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Databricks_Asset_Bundle
  uuid: d0c315ab-e8a4-425f-8fd5-8ac6b70aac39

include:
  - resources/*.yml



# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c900fe0f-c110.cloud.databricks.com/
    variables:
      catalog: abin_catalog
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/erabinas@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/erabinas@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: abin_catalog
      schema: prod
    permissions:
      - user_name: erabinas@gmail.com
        level: CAN_MANAGE
